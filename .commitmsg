feat: wire frontend pages to real SDK, fix SDK type errors

Phase 1: Frontend SDK Integration
- Replace setTimeout mocks in stealth, bridge, compliance pages with real SDK calls
- Extend useHolanc hook with stealthSendTo, stealthScanIncoming, bridgeTransfer,
  discloseToOracle, generateWealthProof
- Export stealth types from SDK index
- Fix ProofStatus mapping for TxStatus -> ProofStatus union
- Add isBusy disabled logic to all action buttons

SDK Type Fixes:
- Fix HolancClient constructor to async factory (HolancWallet.random is async)
- Add await to async wallet calls in client.ts (createDepositNote, computeCommitment, prepareTransfer, prepareWithdraw)
- Fix Groth16Proof field names (pi_a -> piA, pi_b -> piB, pi_c -> piC)
- Fix TS 5.9 Uint8Array/BufferSource compatibility in encryption.ts
- Add snarkjs.d.ts type declarations
- Fix prover.ts return types to allow nested arrays
- Update app tsconfig target to ES2020, include SDK d.ts files
- Remove unavailable BackpackWalletAdapter

Next.js build passes successfully (exit 0), all pages compile.
