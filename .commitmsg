feat: expand CLI with full REPL, on-chain incremental Merkle tree

Phase 2: CLI Expansion & Merkle Tree

CLI:
- Add transfer, withdraw commands with full coin selection
- Add tree info, proof generation, export/import key commands
- Add status overview, command aliases (d/t/w/bal/hist/n/q)
- Pretty-print with box-drawing characters and status indicators
- Show Merkle commitment, root, and leaf info for all operations

Merkle Tree:
- Add next_index() and depth() accessors to holanc-tree
- Add SpendingKey::from_bytes() to holanc-note
- Implement on-chain incremental SHA-256 Merkle tree in pool deposit
- Add filled_subtrees and sha256_root to PoolState
- Pool now maintains both off-chain Poseidon root (via update_root)
  and on-chain SHA-256 root (for consistency verification)

All 47 Rust tests pass. Cargo build clean.
